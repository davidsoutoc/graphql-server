query {
  persons {
    name
    phone
  }
}

======

query {
  personCount
}

======

query {
  persons {
    name
    street
  }
}

======

query($name: String!) {
  findPerson(name: "Josh") {
    phone
  }
}

======

query {
  findPerson(name: "Josh") {
    name
    address
    check
  }
}
